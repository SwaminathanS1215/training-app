import { CustomStructuralDirectiveDirective } from './custom-structural-directive.directive';

describe('CustomStructuralDirectiveDirective', () => {
  it('should create an instance', () => {
  });
});
